import pytest
import numpy as np
import sys
import os

# 将 src 加入路径
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '../src')))

# 注意：由于 lab2_bonus 可能需要学生修改 p1 的代码，此处仅做占位测试
# 实际评分以 lab1_core 为主，lab2 为人工复核
def test_bonus_placeholder():
    assert True
